name: Android CI - Final Build

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Setup Complete Project Structure
      run: |
        echo "🏗️ Setting up complete Android project..."
        
        # 1. إنشاء/إصلاح ملفات Gradle
        mkdir -p gradle/wrapper
        mkdir -p app/src/main/java/com/example/dcacalculator
        mkdir -p app/src/main/res/values
        
        # gradle-wrapper.properties
        cat > gradle/wrapper/gradle-wrapper.properties << 'EOF'
distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
distributionUrl=https\://services.gradle.org/distributions/gradle-8.4-bin.zip
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists
EOF
        
        # build.gradle.kts (الجذر)
        cat > build.gradle.kts << 'EOF'
plugins {
    id("com.android.application") version "8.2.0" apply false
    id("org.jetbrains.kotlin.android") version "1.9.10" apply false
}
EOF
        
        # settings.gradle.kts
        cat > settings.gradle.kts << 'EOF'
pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "DcaCalculator"
include(":app")
EOF
        
        # app/build.gradle.kts (مبسط للغاية)
        cat > app/build.gradle.kts << 'EOF'
plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
}
android {
    namespace = "com.example.dcacalculator"
    compileSdk = 34
    defaultConfig {
        applicationId = "com.example.dcacalculator"
        minSdk = 24
        targetSdk = 34
        versionCode = 1
        versionName = "1.0"
    }
    buildTypes {
        release {
            isMinifyEnabled = false
        }
    }
    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
    kotlinOptions { jvmTarget = "17" }
    buildFeatures { compose = true }
    composeOptions { kotlinCompilerExtensionVersion = "1.5.4" }
}
dependencies {
    implementation("androidx.core:core-ktx:1.12.0")
    implementation("androidx.lifecycle:lifecycle-runtime-ktx:2.7.0")
    implementation("androidx.activity:activity-compose:1.8.2")
    implementation(platform("androidx.compose:compose-bom:2023.08.00"))
    implementation("androidx.compose.ui:ui")
    implementation("androidx.compose.ui:ui-graphics")
    implementation("androidx.compose.material3:material3")
}
EOF
        
        # 2. إنشاء/إصلاح ملفات الموارد
        # AndroidManifest.xml
        cat > app/src/main/AndroidManifest.xml << 'EOF'
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.DcaCalculator">
        <activity
            android:name="com.example.dcacalculator.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.DcaCalculator">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
EOF
        
        # colors.xml
        cat > app/src/main/res/values/colors.xml << 'EOF'
<?xml version="1.0" encoding="utf-8"?>
<resources>
    <color name="black">#FF000000</color>
    <color name="white">#FFFFFFFF</color>
    <color name="purple_200">#FFBB86FC</color>
    <color name="purple_500">#FF6200EE</color>
    <color name="purple_700">#FF3700B3</color>
</resources>
EOF
        
        # themes.xml (مبسط جداً)
        cat > app/src/main/res/values/themes.xml << 'EOF'
<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.DcaCalculator" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/purple_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:statusBarColor">?attr/colorPrimary</item>
    </style>
</resources>
EOF
        
        # strings.xml
        cat > app/src/main/res/values/strings.xml << 'EOF'
<resources>
    <string name="app_name">DCA Calculator</string>
</resources>
EOF
        
        # 3. إذا كان gradlew مفقوداً، استخدم Gradle مباشرة
        echo "✅ Project structure complete!"
    
    - name: Setup JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
    
    - name: Build Debug APK (أسهل للإصلاح)
      run: |
        echo "🚀 Building Debug APK..."
        # استخدام Gradle مباشرة بدون ملفات wrapper
        gradle wrapper --gradle-version 8.4
        chmod +x gradlew
        ./gradlew assembleDebug --no-daemon --stacktrace
        echo "✅ Debug build complete!"
    
    - name: Build Release APK
      run: |
        echo "🚀 Building Release APK..."
        ./gradlew assembleRelease --no-daemon
        echo "✅ Release build complete!"
    
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: DCA-Calculator-APK
        path: app/build/outputs/apk/release/app-release.apk
        retention-days: 7
    
    - name: Upload Debug APK
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: DCA-Calculator-Debug
        path: app/build/outputs/apk/debug/app-debug.apk
        retention-days: 7